.container {
  padding-bottom: 40px;
  padding-top: 40px;
  position: fixed;
  top: 0;
  left: 0;
  height: 100vh;
  width: 270px;
  background-color: var(--white-color);
  box-shadow: var(--shadow-1);
  display: flex;
  flex-direction: column;
  overflow: hidden;
}