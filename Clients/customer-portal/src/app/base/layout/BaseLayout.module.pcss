.container {
  display: flex;
  flex-direction: column;
  min-height: 100vh;
  min-width: 320px;
  background-color: var(--background-color);
}

.body {
  flex-grow: 1;
}

.footer {
  margin-top: auto;
}

